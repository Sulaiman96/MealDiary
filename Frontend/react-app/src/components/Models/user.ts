export type UserProfileToken = {
    firstName: string;
    lastName: string;
    email: string;
    token: string;
}

export type UserProfile = {
    firstName: string;
    lastName: string;
    email: string;
}