import Hero from '../../Components/Hero/hero'

interface Props { }

const HomePage = (props: Props) => {
    return (
        <div>
            <Hero />
        </div>
    )
}

export default HomePage